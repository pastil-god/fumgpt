version: "3.9"

services:
  web:
    image: node:22-alpine
    working_dir: /app
    command: sh -c "corepack enable && corepack prepare pnpm@10.6.0 --activate && pnpm install && pnpm dev --host 0.0.0.0"
    ports:
      - "3000:3000"
    volumes:
      - .:/app
